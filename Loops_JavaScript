# **🚀 الحلقات (Loops) في JavaScript**
تُستخدم الحلقات **لتكرار تنفيذ كود معين** لعدد محدد من المرات أو حتى يتحقق شرط معين.

---

## **1️⃣ `for` Loop**  
يُستخدم عندما تعرف **عدد التكرارات مسبقًا**.
```javascript
for (let i = 0; i < 5; i++) {
    console.log("Iteration:", i);
}
```
### **كيف تعمل؟**  
1️⃣ `let i = 0;` → **تحديد نقطة البداية**  
2️⃣ `i < 5;` → **الشرط** (يستمر التكرار طالما أن `i < 5`)  
3️⃣ `i++` → **زيادة `i` في كل دورة**  

📌 **الإخراج:**
```
Iteration: 0
Iteration: 1
Iteration: 2
Iteration: 3
Iteration: 4
```

---

## **2️⃣ `while` Loop**  
يتم تنفيذ الكود طالما أن الشرط **`true`**.
```javascript
let i = 0;
while (i < 5) {
    console.log("Iteration:", i);
    i++;
}
```
✔ **يُفضل `while` عندما لا تعرف عدد التكرارات مسبقًا.**

---

## **3️⃣ `do...while` Loop**  
**يضمن تنفيذ الكود مرة واحدة على الأقل**، حتى لو كان الشرط `false`.
```javascript
let i = 5;
do {
    console.log("Iteration:", i);
    i++;
} while (i < 5);
```
📌 **الإخراج:**
```
Iteration: 5
```
🔹 **لماذا طُبع `5`؟** → لأن `do` يُنفّذ الكود **قبل** التحقق من الشرط.

---

## **4️⃣ `for...of` Loop**  
يُستخدم مع **المصفوفات (Arrays)** أو **النصوص (Strings)**.
```javascript
let fruits = ["Apple", "Banana", "Mango"];
for (let fruit of fruits) {
    console.log(fruit);
}
```
📌 **الإخراج:**
```
Apple
Banana
Mango
```

---

## **5️⃣ `for...in` Loop**  
يُستخدم لتكرار **خصائص الكائنات (Objects)**.
```javascript
let person = { name: "Sofiane", age: 21, country: "Morocco" };
for (let key in person) {
    console.log(key + ": " + person[key]);
}
```
📌 **الإخراج:**
```
name: Sofiane
age: 21
country: Morocco
```

---

## **6️⃣ `break` و `continue`**
### **🔸 `break` لإنهاء الحلقة فورًا**
```javascript
for (let i = 0; i < 10; i++) {
    if (i === 5) break;
    console.log(i);
}
```
📌 **الإخراج:** `0 1 2 3 4`  
🔹 **عند `i === 5`، يتم الخروج من الحلقة.**

---

### **🔸 `continue` لتخطي التكرار الحالي**
```javascript
for (let i = 0; i < 5; i++) {
    if (i === 2) continue;
    console.log(i);
}
```
📌 **الإخراج:** `0 1 3 4`  
🔹 **عند `i === 2`، يتم تخطي الطباعة، لكن الحلقة تستمر.**

---

## **🎯 متى تستخدم كل نوع من الحلقات؟**
| الحلقة | متى تستخدمها؟ |
|--------|--------------|
| `for` | عندما تعرف عدد التكرارات مسبقًا |
| `while` | عندما لا تعرف عدد التكرارات مسبقًا |
| `do...while` | عندما تريد تنفيذ الكود مرة واحدة على الأقل |
| `for...of` | عندما تتعامل مع **مصفوفة أو نص** |
| `for...in` | عندما تتعامل مع **كائن (Object)** |

---

## **🔥 تمارين تطبيقية**
1. استخدم **`for`** لطباعة الأعداد من **1 إلى 10**.
2. استخدم **`while`** لطباعة الأعداد **الزوجية** من **2 إلى 10**.
3. استخدم **`for...of`** لطباعة جميع عناصر المصفوفة `["Red", "Green", "Blue"]`.
4. استخدم **`for...in`** لطباعة جميع خصائص الكائن `{brand: "Toyota", model: "Corolla", year: 2024}`.
5. استخدم **`break`** لإنهاء الحلقة عند الوصول إلى الرقم **7** في حلقة **`for`** من 0 إلى 10.

---

